import { View } from 'react-native';

export default function Divider() {
  return (
    <View
      style={{
        borderColor: '#FFFFFF33',
        borderTopWidth: 1,
        marginVertical:8
      }}
    />
  );
}
